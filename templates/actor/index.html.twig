{% extends 'base.html.twig' %}

{% block title %}Tout les acteurs{% endblock %}

{% block body %}
    <h1>Toutes les acteurs de la table Actor : </h1>
    <ul>
    {% for actor in actors %}
        <div>
            <li>
                <a href="{{ path('actor_show', { id: actor.id }) }}">
                    {{ actor.name }}
                </a>
            </li>
        </div>
    {% else %}
        Aucun acteur trouvé.
    {% endfor %}
    </ul>
    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}

{# {% extends 'base.html.twig' %}

{% block title %}Actor index{% endblock %}

{% block body %}
    <h1>Actor index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Picture</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for actor in actors %}
            <tr>
                <td>{{ actor.id }}</td>
                <td>{{ actor.name }}</td>
                <td>{{ actor.picture }}</td>
                <td>
                    <a href="{{ path('actor_show', {'id': actor.id}) }}">show</a>
                    <a href="{{ path('actor_edit', {'id': actor.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('actor_new') }}">Create new</a>
{% endblock %}
 #}