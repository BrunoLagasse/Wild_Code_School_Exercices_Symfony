{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ comment.comment }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ comment.rate }}</td>
            </tr>
        </tbody>
    </table>

    <button><a href="{{ path('comment_edit', {'id': comment.id}) }}">Modifier le commentaire</a></button><br>
    {{ include('comment/_delete_form.html.twig') }}

    <a href="{{ path('comment_index') }}">Retour à la liste des commentaires</a>
{% endblock %}
